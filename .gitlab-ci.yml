stages:
  - prepare

prepare:
  image: ubuntu:latest
  script:
    - echo "Preparing the environment"
    - apt-get update
    - apt-get install -y cmake g++ python3-pip
    - pip install conan
  only:
    - master
